# 🚀 Настройка Git репозитория

## 📋 Что уже готово:

✅ `.gitignore` - исключены временные файлы, логи, docs/*.pdf  
✅ `requirements.txt` - полный список зависимостей  
✅ `LICENSE` - MIT лицензия  
✅ `README.md` - описание проекта  

## 🔄 Команды для Git:

```bash
# Инициализация репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: IDOR Pwn - продвинутый детектор IDOR уязвимостей

- Реализован многостратегийный подход к детекции
- Создан веб-интерфейс на русском языке
- Добавлена система логирования и отчетов
- Настроены конфигурации для разных окружений

Автор: Смирных Павел Ильич, МБОУ Гимназия №2, г. Сургут, 2026"

# Добавление удаленного репозитория (замени на свой)
git remote add origin https://github.com/your-username/idor-pwn.git

# Отправка в репозиторий
git push -u origin main
```

## 📁 Структура проекта в Git:

```
idor_pwn/
├── .gitignore              # ✅ Исключения
├── requirements.txt        # ✅ Зависимости  
├── LICENSE                # ✅ Лицензия
├── README.md             # ✅ Описание
├── setup.py              # ✅ Установка
├── run.bat / run.sh      # ✅ Запуск
├── victim_api.py         # ✅ Тестовый API
├── core/                 # ✅ Модули детекции
├── auth/                 # ✅ Аутентификация
├── web/                  # ✅ Веб-интерфейс
├── config/               # ✅ Конфигурации
├── examples/             # ✅ Примеры
└── docs/                # 📝 Доки (без PDF)
    ├── *.tex            # ✅ LaTeX исходники
    ├── *.bib            # ✅ Библиография
    ├── Makefile         # ✅ Для Linux/Mac
    ├── build.ps1        # ✅ Для PowerShell
    ├── build.bat        # ✅ Для Windows
    └── README.md       # ✅ Инструкция
```

## 🎯 Что НЕ попадет в Git:

❌ `docs/*.pdf` - скомпилированные документы  
❌ `logs/` - логи работы  
❌ `venv/` - виртуальное окружение  
❌ `__pycache__/` - кэш Python  
❌ `*.pyc` - скомпилированный Python  
❌ `.env` - переменные окружения  

## 🚀 Быстрый старт для клонирования:

```bash
# Клонирование
git clone https://github.com/your-username/idor-pwn.git
cd idor-pwn

# Установка
python setup.py

# Запуск
run.bat  # Windows
./run.sh  # Linux/Mac
```

## 📝 Документация позже:

Документация в `docs/` готова, но PDF файлы не в Git. Потом добавишь:
```bash
# После компиляции доков
git add docs/*.pdf
git commit -m "Add compiled documentation"
git push
```

## 🔧 Проверка перед отправкой:

```bash
# Проверка статуса
git status

# Проверка .gitignore
git check-ignore -v *

# Тестовая установка
pip install -r requirements.txt
python -c "from core.advanced_detector import AdvancedIDORDetector; print('✅ OK')"
```

**Готов к отправке в Git! 🚀**
